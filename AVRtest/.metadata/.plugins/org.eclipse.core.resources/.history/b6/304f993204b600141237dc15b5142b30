#include <stdlib.h>

int main(void) {
	for (int y0 = iY - 1; y0 <= iY + 1; y0++) {
		for (int x0 = iX - 1; x0 <= iX + 1; x0++) {
			if (((tBuffer[y0 % SIZE] & (1 << (7 - (x0 % SIZE))))
					>> (7 - (x0 % SIZE))) == 1)
				n++;
		}
	}

	return 0;
}
